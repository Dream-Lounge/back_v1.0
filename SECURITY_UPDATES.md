# 보안 및 운영 변경 기록

## 2026-09-13

### Python 의존성 갱신

`pip-audit`에서 확인된 네트워크, 인증, 암호화 및 파일 업로드 계층의 보안 권고를 반영하기 위해 `uv.lock`을 갱신했다. 운영 의존성은 `pyproject.toml`에 정확한 버전으로 고정해 로컬과 자동 배포가 동일한 패키지 조합을 사용하도록 했다.

주요 변경:

- FastAPI `0.128.0` → `0.141.1`
- Starlette `0.50.0` → `1.6.0`
- Supabase Python `2.28.3` → `2.31.0`
- cryptography `46.0.3` → `50.0.1`
- PyJWT `2.12.1` → `2.14.0`
- python-multipart `0.0.21` → `0.0.32`
- urllib3 `2.6.2` → `2.7.0`
- h2 `4.3.0` → `4.4.1`
- SQLAlchemy `2.0.45` → `2.0.52`
- Alembic `1.18.4` → `1.20.0`

`fastapi[all]`은 운영 API에 사용하지 않는 CLI 및 배포 도구까지 설치하므로 기본 `fastapi`와 실제 파일 업로드에 필요한 `python-multipart` 조합으로 축소했다.

재검사 결과 기존의 주요 보안 권고는 해소되었다. `pip-audit`은 동일한 `PYSEC-2026-1325`를 중복으로 2건 표시했다. `python-ecdsa 0.19.2`에는 수정 버전이 없는 P-256 타이밍 공격 권고가 남아 있지만, 해당 권고는 개인키 서명·키 생성·ECDH에 영향을 주며 서명 검증에는 영향을 주지 않는다. 이 서비스는 Supabase 공개키를 이용한 JWT 서명 검증에만 해당 경로를 사용한다.

### DB 연결 풀

- 컨테이너별 기본값: `DB_POOL_SIZE=12`, `DB_MAX_OVERFLOW=8`
- 컨테이너 2개 기준 이론상 최대 백엔드 연결: 40개
- `dreamlounge_backend` 역할의 기존 `CONNECTION LIMIT 10`을 50으로 올리는 Alembic 마이그레이션 `0021` 추가
- 실제 Supabase 반영에는 관리자 연결로 `alembic upgrade head` 실행이 필요하다.

### 검증

- Python 3.12 격리 환경 전체 테스트: `160 passed`
- Alembic revision: 단일 `0021 (head)` 확인
- 취약점 재검사: 운영에 사용되는 주요 취약 패키지 권고 해소, 위 `python-ecdsa` 검증 전용 예외만 잔존

### 개인정보 파기

개인정보 자동 파기는 현재 운영 범위에 포함하지 않는다. 모집 종료 후 파기 방식과 일정을 확정하기 전까지 별도 자동 삭제 작업은 추가하지 않았다.
